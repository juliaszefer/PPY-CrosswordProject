class Level:
    def __init__(self, id_level, title, min_points, max_points):
        self.id_level = id_level
        self.title = title
        self.min_points = min_points
        self.max_points = max_points
